package org.oxycblt.auxio.car.overlay

/**
 * Placeholder test file.
 *
 * Codex should replace this with Robolectric or instrumentation tests matching the existing Auxio test stack.
 * Suggested assertions:
 * - defaults are sensible;
 * - opacity/button sizes are clamped;
 * - resetPosition restores defaults;
 * - enable/disable persists.
 */
class CarOverlayPrefsContractTest
